import Layout from "./Layout"

export const Main = () => {
    return (
        <Layout>
            <div className="container">
                is this working?
        </div>
        </Layout>
    )
}